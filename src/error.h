#ifndef __ERROR__
#define __ERROR__
class Error {
    map<string,something> mp
    
    
    duplicate labels
    (instruction name not valid) OR (labels have other params after it)
    correct instruction, but wrong number of args
    parameters have unassigned variables
    invalid variable name ie: 8aeb (only start with alpha bet)
    invalid parameters, like passing in a label instead of imm. val or variables
    
    Error() {
        
    }
};
#endif